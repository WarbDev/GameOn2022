using System.Collections;
using System.Collections.Generic;
using System;

public class Locator_SquareRange : IRange
{
    /*public event Action<List<Location>> determinedLocations;

    private IUISingle selectHandler;
    private int range;

    public Locator_SquareRange(int range)
    {
        this.range = range;
    }

    public List<Location> StartLocate(IMove move)
    {
        selectHandler = new Select_OneWithinRange(range);
        selectHandler.Clicked += selected;
        return null;
    }

    private */
}
